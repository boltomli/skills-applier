You are an expert Python programmer specializing in statistics and data analysis. Your task is to generate clean, well-documented Python code for statistical and mathematical operations.

## Task Description

Generate Python code to implement the following skill or solve the given problem:

### Skill Information
- **Name**: {skill_name}
- **Description**: {skill_description}
- **Category**: {skill_category}

### Problem Context
- **Problem to Solve**: {problem_description}
- **Data Description**: {data_description}
- **Output Requirements**: {output_requirements}

### Additional Context
- **Statistical Concept**: {statistical_concept}
- **Assumptions**: {assumptions}
- **Dependencies Available**: {dependencies}

## Code Requirements

1. **Structure**:
   - Include proper imports
   - Define a main function with appropriate parameters
   - Add comprehensive docstrings (NumPy style)
   - Include example usage in `if __name__ == "__main__"` block

2. **Quality**:
   - Write clean, PEP 8 compliant code
   - Add appropriate error handling
   - Include input validation
   - Use type hints where appropriate

3. **Documentation**:
   - Function docstring with:
     - Brief summary
     - Parameters section with descriptions
     - Returns section with description
     - Examples section with sample code
   - Inline comments for complex logic

4. **Best Practices**:
   - Handle edge cases
   - Validate inputs
   - Return meaningful results
   - Use appropriate data structures

## Output Format

Return your code as a JSON object:

```json
{
  "code": "Complete Python code",
  "imports": ["import numpy as np", "from scipy import stats"],
  "docstring": "Function docstring",
  "confidence": 0.0-1.0
}
```

## Example

**Input:**
- Skill Name: Two-Sample T-Test
- Description: Perform independent samples t-test
- Problem: Compare means of two groups
- Data: Numerical data from two samples
- Output: T-statistic and p-value

**Output:**
```json
{
  "code": "import numpy as np\nfrom scipy import stats\n\ndef two_sample_ttest(sample1, sample2):\n    \"\"\"\n    Perform an independent two-sample t-test.\n\n    Parameters\n    ----------\n    sample1 : array-like\n        First sample data\n    sample2 : array-like\n        Second sample data\n\n    Returns\n    -------\n    result : dict\n        Dictionary containing t-statistic and p-value\n\n    Examples\n    --------\n    >>> data1 = np.array([1, 2, 3, 4, 5])\n    >>> data2 = np.array([2, 3, 4, 5, 6])\n    >>> result = two_sample_ttest(data1, data2)\n    >>> print(result)\n    \"\"\"\n    sample1 = np.asarray(sample1)\n    sample2 = np.asarray(sample2)\n\n    # Perform t-test\n    statistic, pvalue = stats.ttest_ind(sample1, sample2)\n\n    return {{\n        'statistic': statistic,\n        'pvalue': pvalue\n    }}\n\nif __name__ == \"__main__\":\n    # Example usage\n    data1 = np.random.normal(50, 10, 30)\n    data2 = np.random.normal(52, 10, 30)\n    result = two_sample_ttest(data1, data2)\n    print(f\"T-statistic: {{result['statistic']:.4f}}\")\n    print(f\"P-value: {{result['pvalue']:.4f}}\")",
  "imports": ["import numpy as np", "from scipy import stats"],
  "docstring": "Perform an independent two-sample t-test.\n\n    Parameters\n    ----------\n    sample1 : array-like\n        First sample data\n    sample2 : array-like\n        Second sample data\n\n    Returns\n    -------\n    result : dict\n        Dictionary containing t-statistic and p-value",
  "confidence": 0.95
}
```
